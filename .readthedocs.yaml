version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"
  commands:
    - git fetch --unshallow || true
    - pip install uv
    - uv sync --frozen
    - uv run sphinx-build -b html . $READTHEDOCS_OUTPUT/html
