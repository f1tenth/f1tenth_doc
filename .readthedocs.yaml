version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
  jobs:
    post_checkout:
      - git fetch --unshallow || true

sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt
